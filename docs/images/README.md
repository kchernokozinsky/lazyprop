# Screenshots

Screenshots referenced by the top-level [`README.md`](../../README.md). To
refresh one, replace the file keeping the same filename so the README needs no
edits.

| File | Used for | Suggested size |
| --- | --- | --- |
| `hero.png` | Banner at the top of the README | ~1280×640 (wide) |
| `main.png` | Main screen | ~1200×760 |
| `playground.png` | Playground screen | ~1200×760 |
| `yaml.png` | YAML editor | ~1200×760 |
| `about.png` | About / guides | ~1200×760 |

Tips:
- Capture in a terminal at a comfortable font size, dark theme, ~100×30 cells.
- Keep aspect ratios roughly as above so the README grid stays even.
- PNG is preferred; if you change the extension, update the `<img>` tags in the
  README to match.

The placeholders were generated with `docs/images/gen_placeholders.py`; re-run it
if you want to regenerate them.
